class song:
    def __init__(self,data):
        self.data=data
        self.next=None
class Playlist:
    def __init__(self):
        self.head=None
    def is_empty(self):
        return self.head is None
    def append(self,data):
        new_node=song(data)
        if self.head is None:
            self.head=new_node
            return
        current=self.head
        while current.next:
            current=current.next
        current.next=new_node
    def prepend(self,data):
        new_node=song(data)
        new_node.next=self.head
        self.read=new_node
    def delete(self,data):
        if self.head is None:
            return
        if self.head.data==data:
            self.head=self.head.next
            return
        current=self.head
        while current.next:
            if current.next.data==data:
                current.next=current.next.next
                return
            current=current.next
    def search(self,data):
        current=self.head
        while current:
            if current.next.data==data:
                return True
            current=current.next
        return False
    def display(self):
        current=self.head
        while current:
            print(current.data,end="->")
            current=current.next
        print("No song")
    def insert(self,data,prev):
        new_song=song(data)
        current=self.head
        while current:
            if current.data==prev:
                new_song.next=current.next
                current.next=new_song
                return
            current=current.next
        return
pt=Playlist()
pt.append("kaithi")
pt.append("leo")
pt.append("vikram")
print("playlist:")
pt.display()
pt.prepend("dc")
pt.prepend("benz")
print("prepended song list:")
pt.display()
pt.delete("vikram")
print("After delete")
pt.display()
pt.insert("kaithi","master")
print("new playlist inserted")
pt.display()
